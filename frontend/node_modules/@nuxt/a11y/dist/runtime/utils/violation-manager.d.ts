import type axe from 'axe-core';
import type { A11yViolation } from '../types.js';
/**
 * Manages accessibility violations: tracking, merging, and deduplication.
 * Maintains separate violation stores per browser tab for complete isolation.
 */
export declare function createViolationManager(): {
    processViolations: (violations: axe.Result[], scanRoute: string, tabId: string) => A11yViolation[];
    getAll: (tabId: string) => A11yViolation[];
    reset: (tabId: string) => void;
    resetAll: () => void;
};
