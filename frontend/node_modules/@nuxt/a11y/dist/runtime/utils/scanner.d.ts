/**
 * Manages event listeners and debouncing for constant accessibility scanning.
 */
export declare function createScanner(onScan: () => void): {
    enable: () => void;
    disable: () => void;
    isEnabled: () => boolean;
};
